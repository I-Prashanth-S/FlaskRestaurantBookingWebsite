# import sqlite3 as sql
#
# con=sql.connect("database.db")
# cur=con.cursor()
# cur.execute('''create table user(username varchar(20),password varchar(20),email varchar(20))''')
# con.commit()
# con.close()